{% set name = "bravado-core" %}
{% set version = "5.12.0" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 2e5a59dbacbd3a393195eabc6aac7b9c3fcac439c6c8b2186a0567d2da9e8a7b

build:
  number: 0
  script: "{{ PYTHON }} -m pip install . --no-deps --ignore-installed --no-cache-dir -vvv "
  noarch: python

requirements:
  host:
    - jsonref
    - jsonschema[format] >=2.5.1
    - msgpack-python
    - pip
    - python
    - python-dateutil
    - pytz
    - pyyaml
    - simplejson
    - six
    - swagger-spec-validator >=2.0.1
  run:
    - jsonref
    - jsonschema[format] >=2.5.1
    - msgpack-python
    - python
    - python-dateutil
    - pytz
    - pyyaml
    - simplejson
    - six
    - swagger-spec-validator >=2.0.1

test:
  imports:
    - bravado_core

about:
  home: https://github.com/Yelp/bravado-core
  license: BSD
  license_family: BSD
  license_file: LICENSE.txt
  summary: Library for adding Swagger support to clients and servers

extra:
  recipe-maintainers:
    - chapmanb
    - marcelotrevisani
